# URL-Shortener-with-Analytics
# URL Shortener with Analytics 🚀

A full-stack web application built with **Flask** and **SQLAlchemy** that shortens long URLs and tracks click analytics.

## ✨ Features
- **URL Shortening:** Converts long URLs into 5-character alphanumeric codes.
- **Custom Aliases:** Users can choose their own short links (e.g., `/my-link`).
- **Click Tracking:** Real-time counter to see how many times a link has been clicked.
- **Validation:** Ensures URLs are valid (HTTP/HTTPS) using Regex.
- **Responsive UI:** Built with Bootstrap 5 for mobile and desktop support.

## 🛠️ Tech Stack
- **Backend:** Python (Flask)
- **Database:** SQLite with SQLAlchemy ORM
- **Frontend:** HTML5, CSS3, Bootstrap 5, Jinja2

## 🚀 How to Run Locally

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/YOUR_USERNAME/url-shortener-flask.git](https://github.com/YOUR_USERNAME/url-shortener-flask.git)
